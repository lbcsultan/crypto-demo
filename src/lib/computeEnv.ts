import forge from 'node-forge'

const genEnv = (plaintext: string, sender: string, receiver: string) => {}

const openEnv = (ciphertext: string, sender: string, receiver: string) => {}

export { genEnv, openEnv }
